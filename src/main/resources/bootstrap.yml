spring:
  application:
    name: retail-bank-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/amitaucs/Y_Microservice_Retail_Bank_properties.git
          search-paths:
            - retailBank*
          username:
          password:
          default-label: main
server:
  port: 8333